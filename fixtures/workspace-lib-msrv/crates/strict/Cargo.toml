# SPDX-FileCopyrightText: RUST-TOOLCHAIN-MATRIX contributors
#
# SPDX-License-Identifier: MIT OR Apache-2.0

# SHAPE 3 -- local override ABOVE the workspace floor. The effective MSRV for the
# tree is the maximum across the graph (1.92), not the workspace value (1.88).

[package]
name = "strict"
description = "Demo fixture member crate for the rust-toolchain-matrix action"
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
version.workspace = true
rust-version = "1.92"
publish = false

[dependencies]

[lints]
workspace = true
